# Фото — тяжёлые и эфемерные (чистятся через PHOTO_RETENTION_DAYS), не версионируем.
# Данные (data/, profile.md, reference/) НАМЕРЕННО под git — это бэкап и история.
photos/

# Логи фоновой обработки
scripts/*.log

# Системное
.DS_Store
